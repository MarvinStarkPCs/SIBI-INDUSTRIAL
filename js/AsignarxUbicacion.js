// Evento para detectar cuando se selecciona una ubicación
$('#origen').change(function() {
    // Verificar si está visible el formulario de "Por Ubicación"
    if ($('#ubicacionForm').is(':visible')) {
    }
});